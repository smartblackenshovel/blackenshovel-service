# BlackenShovel Service

## Overview
This project is a backend service to manage organizations, shovels, users, sessions, spots, and logs. Built with Go, GORM, and PostgreSQL.

---

## Requirements

- Docker & Docker Compose
- Go (if you want to run outside Docker)

---

## Run with Docker

1. Build and start containers:

```bash
docker-compose up --build
